{
  "name": "kysely-plugins",
  "version": "0.0.3",
  "private": false,
  "license": "MIT",
  "scripts": {
    "build": "npm run build:auto-updated-at && npm run build:soft-delete",
    "build:auto-updated-at": "tsdown --config src/auto-updated-at/tsdown.config.ts",
    "build:soft-delete": "tsdown --config src/soft-delete/tsdown.config.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:ci": "vitest run",
    "migrate:make": "kysely migrate make",
    "migrate:latest": "kysely migrate latest",
    "migrate:generate-orm": "kysely-codegen generate --out-file ./src/utils/db/db.d.ts"
  },
  "devDependencies": {
    "@types/pg": "^8.16.0",
    "dotenv": "^17.3.1",
    "husky": "^9.1.7",
    "kysely": "^0.28.11",
    "kysely-codegen": "^0.20.0",
    "kysely-ctl": "^0.20.0",
    "pg": "^8.18.0",
    "prettier": "^3.8.1",
    "tsdown": "^0.21.4",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
